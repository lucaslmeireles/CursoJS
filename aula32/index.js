// for classico
   // declarada a var condição do laço o que fazer a cada volta

// for (let i = 0; i <= 10 ; i++) {
//     const par = i % 2 === 0  ? 'par' : 'impar';
//     console.log(par);
// }

const frutas = [ 'pera', 'maça', 'uva'];
for (let i= 0 ; i < frutas.length; i++) {
    console.log(frutas[i]);
}